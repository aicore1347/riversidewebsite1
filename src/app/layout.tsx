import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/header";
import Footer from "@/components/layout/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Riverside AI | Transforming Businesses with AI",
  description: "AI solutions for companies looking to scale their business. Transform your operations with cutting-edge artificial intelligence technology.",
  keywords: "AI, artificial intelligence, business automation, machine learning, digital transformation, business scaling",
  authors: [{ name: "Riverside AI" }],
  openGraph: {
    title: "Riverside AI | Transforming Businesses with AI",
    description: "AI solutions for companies looking to scale their business",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-riverside-black text-riverside-white min-h-screen`}
      >
        <Header />
        <main className="pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
